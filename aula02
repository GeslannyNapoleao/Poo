class Aluno:

    def __init__(self, nome, matricula):
        # Atributos privados
        self.__nome = None
        self.__matricula = None
        self.__notas = []

        # Usando os setters para validar os dados
        self.set_nome(nome)
        self.set_matricula(matricula)

    # Getter para o nome
    def get_nome(self):
        return self.__nome

    # Setter para o nome com validação
    def set_nome(self, nome):
        if nome and nome.strip():
            self.__nome = nome
        else:
            print("Nome inválido. Por favor, insira um nome válido.")

    # Getter para matrícula
    def get_matricula(self):
        return self.__matricula

    # Setter para matrícula com validação
    def set_matricula(self, matricula):
        if matricula.isdigit() and 8 <= len(matricula) <= 10:
            self.__matricula = matricula
        else:
            print("Matrícula inválida. Deve conter entre 8 e 10 dígitos.")

    # Método para adicionar nota
    def adicionar_nota(self, nota):
        if 0 <= nota <= 10:
            self.__notas.append(nota)
        else:
            print("Nota inválida! A nota deve estar entre 0 e 10.")

    # Método para calcular média
    def calcular_media(self):
        if len(self.__notas) == 0:
            return 0
        return sum(self.__notas) / len(self.__notas)
    

    # Método solicitado no exercício
    def mostrar_dados(self):
        print(f"Nome: {self.get_nome()}")
        print(f"Matrícula: {self.get_matricula()}")
        print(f"Média: {self.calcular_media():.2f}")
        print("-" * 30)
        print("nota inválida")


# Aluno 1
aluno1 = Aluno("João", "20251010")
aluno1.adicionar_nota(8.0)
aluno1.adicionar_nota(7.0)

# Aluno 2
aluno2 = Aluno("Eduarda Mendes", "20251011")
aluno2.adicionar_nota(9.5)
aluno2.adicionar_nota(8.5)

# Aluno 3
aluno3 = Aluno("Claúdio", "20251012")
aluno3.adicionar_nota(6.0)
aluno3.adicionar_nota(7.5)

# Mostrando dados
aluno1.mostrar_dados()
aluno2.mostrar_dados()
aluno3.mostrar_dados()
  


class AlunoIntegrado(Aluno):
    def __init__(self, nome, matricula):
        super().__init__(nome, matricula)
        self.curso_tecnico = curso_tecnico

        def mostar_dados(self):
            super().mostar_dados()
            print(f"curso Tecnico:{self.curso_tecnico})")

class AlunoGraduação(Aluno):
    def __init__(self, nome, matricula, curso_graduacao):
        super().__init__(nome, matricula, curso_graduacao)
        self.curso_graduacao.curso_graduacao = curso_garduacao

        def mostra_dados(self):
            super().mostrar_dados()
            print(f"curso de graduapção:{self.curso_graduacao}")

            
aluno1  = AlunoIntegrado("João", "20251010", "informática")
aluno02 = AlunoGraduação("Claudio", "20490010", "Sistema informacao")

aluno1.adicionar_nota(8.0)
aluno2.adicionar_nota(7.0)

aluno1.adicionar_nota(8.0)
aluno2.adicionar_nota(7.0)